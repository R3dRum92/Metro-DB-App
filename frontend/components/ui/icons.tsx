import { Loader2, Lock, CheckCircle, LogOut, User, Users, Settings, Check } from "lucide-react"

export const Icons = {
  spinner: Loader2,
  lock: Lock,
  check: CheckCircle,
  logout: LogOut,
  user: User,
  users: Users,
  settings: Settings
}

